# Middleware server
PORT=3001
ENGINE_URL=http://localhost:5000/analyze
# Optional shared key; when set, frontend must send header x-wce-key with this value
WCE_SHARED_KEY=super-secret-key
# Comma-separated exact origins, e.g., http://localhost:5173,http://localhost:4173
# Note: Any localhost port is allowed by default for dev (http://localhost:<any>, http://127.0.0.1:<any>)
CORS_ORIGIN=http://localhost:5173

# Engine server
ENGINE_PORT=5000
